---
interface Props {
  src: string
  alt: string
  className?: string
}

const { alt, src, className } = Astro.props as Props
---

<figure
  class={`relative w-full h-full z-[1] after:absolute after:w-full after:h-full after:-top-2 after:-left-3 after:rounded-tl-[90px] after:rounded-tr-xl after:rounded-b-[196px] after:-z-[1] after:border after:border-slate-400 after:min-h-[406px] before:absolute before:w-full before:-top-2 before:-left-6 before:rounded-tl-[90px] before:rounded-tr-xl before:rounded-b-[196px] before:-z-[1] before:border before:border-slate-400 before:min-h-[431px] ${className}`}
>
  <img
    src={src}
    alt={alt}
    class='rounded-tl-[90px] rounded-tr-xl rounded-b-[196px] object-cover h-full min-h-[392px]'
  />
</figure>
